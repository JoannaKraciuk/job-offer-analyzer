# Data Directory

This directory is intentionally kept in the repository, but real Excel workbooks are ignored.

Do not commit private job-application data, links, notes, backups, or generated `.xlsx` files.

Use this directory locally for:

- `baza_ofert_pracy_QA.xlsx`
- temporary workbook copies
- local backups
